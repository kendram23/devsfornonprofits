<h1><%= @project.title %></h1>
<h2>Posted By: <%= link_to current_user.company_name, "#" %></h2>
<p><%= @project.description %></p>
<p><%= @project.requirements %></p>
<p><%= @project.project_deadline %></p>
<p><%= @project.category %></p>

<p>

<% if current_user.role == 'nonprofit' && @project.user_id == current_user.id %>
	<%= link_to "Edit Project", edit_project_path(@project) %>
<% end %>
<% if current_user.role == 'nonprofit' && @project.user_id == current_user.id %>
	<%= link_to "Delete Project", @project, :method => :delete, :confirm => "Are you sure you want to delete this project? This action can not be undone." %>
<% end %>

<% if current_user.role == 'developer' %>
<% end %>

<%= link_to "Back to Projects", projects_path %>
<p>


<h1> Apply for: <%= @project.title %></h1>

<p>Nulla hizzle ligula. Mammasay mammasa mamma oo sa porta commodo tellus. Aenean viverra, stuff izzle away black, libero shizzlin dizzle owned leo, non check it out nunc sapien shit nunc. Tellivizzle eu go to hizzle. Vestibulum break it down izzle. Sed elementizzle i'm in the shizzle eros. Stuff nulla things, i'm in the shizzle izzle, volutpizzle mofo, auctor egizzle, nunc. Mammasay mammasa mamma oo sa stuff. My shizz nisi. My shizz gizzle amizzle leo nec ante blandit dignissizzle. Fizzle laorizzle shizzlin dizzle sizzle fo shizzle enim. Black you son of a bizzle sure felizzle.</p>

<% form_for ([@project, @project.current_projects.build]) do |f| %> 

       <%= f.hidden_field :user_id, :value => 
       current_user.id %>
       <%= f.hidden_field :project_id, :value => @project.id %>
  <div>
    <%= f.submit "Submit" %></div>
<% end %>


